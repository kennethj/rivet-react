import * as PropTypes from 'prop-types';
import { getPropTypes } from './propTypes'

// describe("propTypes", () => {
//     it ("should parse propTypes", () => {
//         const actual = getPropTypes(`${__dirname}/../Badge/Badge.tsx`);
//         expect(actual).toBe({
//             role: PropTypes.oneOf(['default', 'secondary']),
//             variant: PropTypes.oneOf(['', 'action', 'error', 'success', 'warning'])
//         })
//     });
// });
